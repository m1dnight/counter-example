[package]
name = "counter_witness"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0"
rustler = "0.36.2"
arm = { git = "https://github.com/anoma/arm-risc0", package = "arm", tag = "v0.3.0", features = ["logic_circuit"], default-features = false }
